// core/bitmask.js

// Example constant, if you limit devices per group using a bitmask approach
export const MAX_DEVICES_PER_GROUP = 32; // Or 64, depending on your needs

/**
 * Finds the next available bit position in a bitmask.
 * (This is a placeholder - implement actual bitmask logic if needed)
 * @param {number} currentMask - The current bitmask.
 * @returns {number} The position (0-indexed) of the next available bit, or -1 if full.
 */
export function getNextAvailableBitPosition(currentMask) {
  // Placeholder implementation
  // For a real implementation, you'd iterate and check bits.
  console.warn("getNextAvailableBitPosition is a placeholder and not fully implemented.");
  return -1;
}